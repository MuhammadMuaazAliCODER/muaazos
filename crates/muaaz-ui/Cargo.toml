[package]
name = "muaaz-ui"
version = "0.1.0"
edition = "2024"
description = "Reusable UI components, styling, and design system for Muaaz OS"
license = "MIT"

[dependencies]
gtk = { package = "gtk4", version = "0.11", features = ["v4_16"] }